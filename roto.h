#ifndef ROTO_H_INCLUDED
#define ROTO_H_INCLUDED

#include <SDL/SDL.h>
#include <SDL/SDL_image.h>
#include <SDL/SDL_rotozoom.h>

SDL_Surface * ESDeathAnimation(double * zoom, SDL_Surface * image);

#endif